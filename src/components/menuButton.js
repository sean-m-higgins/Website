import React, { Component } from "react";
import '../styles/menuButton.css';

class NavigationButton extends Component {
  render() {
    return (
      <button onMouseDown={this.props.handleMouseDown}>=</button>
    );
  }
}

export default NavigationButton;